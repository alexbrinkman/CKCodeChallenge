<h1>Box Office Projections for the Weekend starting:</h1>

<h2>Ranking on date:</h2>

<table>
  <tr>
    <% @weeks.each do |date| %>
      <th><%= date %></th>
    <% end %>
      <th>&nbsp;</th>
  </tr>
  <% @movies.each do |movie| %>
    <tr>
      <td><%= movie[:week1] %></td>
      <td><%= movie[:week2] %></td>
      <td><%= movie[:week3] %></td>
      <td><%= movie[:week4] %></td>
      <td><%= movie[:title] %></td>
    </tr>
  <% end %>
</table>